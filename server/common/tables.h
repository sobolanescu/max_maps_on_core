In:

typedef struct SPacketGDSetup

Search:

	long	alMaps[32];

Change in:

	long	alMaps[MAX_MAP_ALLOW];

In:

typedef struct SMapLocation

Search:

	long	alMaps[32];

Change in:

	long	alMaps[MAX_MAP_ALLOW];
